extends ../layout

block content 
    h1 User: #{user.name}

    p Are you sure?
    form(method='POST')
        input#userID(type='hidden' name='userID', value=user.id )
        button.btn.btn-danger Delete
